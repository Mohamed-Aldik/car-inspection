import { Layout } from "../../layout";
import { HeroSection } from "./components/HeroSection";

const LandingPage = () => {
  return (
    <Layout>
      <HeroSection />
    </Layout>
  );
};

export default LandingPage;
